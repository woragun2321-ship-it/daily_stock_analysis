# 📈 {{ report_date }} Trade Plan

> {{ results|length }} ตัว | 🟢{{ buy_count }} 🟡{{ hold_count }} 🔴{{ sell_count }}
{% if market_status_line %}
{{ market_status_line }}
{% endif %}

{% for e in enriched %}
{% set dash = e.result.dashboard or {} %}
{% set core = (dash.get('core_conclusion') or {}) if dash else {} %}
{% set plan = (dash.get('action_plan') or {}) if dash else {} %}
{% set points = (plan.get('price_points') or {}) if plan else {} %}

## {{ e.stock_name }} ({{ e.result.code }}) {{ e.signal_emoji }}

**Action:** {{ e.localized_operation_advice }}  
**Score:** {{ e.result.sentiment_score }}  
**Summary:** {{ (core.get('one_sentence') or e.result.analysis_summary or '')[:90] }}

**Entry:** {{ points.get('ideal_buy') or 'N/A' }}  
**Alt Entry:** {{ points.get('secondary_buy') or 'N/A' }}  
**Stop Loss:** {{ points.get('stop_loss') or 'N/A' }}  
**Target:** {{ points.get('target') or 'N/A' }}

---
{% endfor %}

*{{ report_timestamp }}*
{% if show_llm_model and models_used %}
*{{ labels.analysis_model_label }}: {{ models_used|join(', ') }}*
{% endif %}
